# Port for the firmware API
MC_FW_PORT=5000

# Require API key on requests (true/false). When true, header X-API-KEY must match MC_FW_API_KEY
MC_FW_REQUIRE_API_KEY=false

# If API key requirement is enabled, provide the expected key
MC_FW_API_KEY=changeme
